import Pessoa_a, { Objeto, Coisas } from "./031-classe-modulo.js";

const p1 = new Pessoa_a('RD3W', 22);
const obj1 = new Objeto('Pessoa')
const coisa = Coisas

console.log(p1.nome)
console.log(obj1.nome)
console.log(coisa.length, coisa[0])